jQuery(function($) {
  if ($('body').hasClass('home')) {
    if ($('#featured li').size() > 1) {
      return $('#featured img').first().load(function() {
        return $('#featured').unslider({
          delay: 4 * 1000,
          fluid: true
        });
      });
    }
  }
});
